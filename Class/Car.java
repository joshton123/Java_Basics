public class Car {
    int driver;
    
    public static void Drive(){
        System.out.println("Driving");
    }
}

// Paramterized constructor car("red") . then we have to go give a variable or it is error
// static {
    // this will run first because it will be static
// }
// when object is called this will run first
